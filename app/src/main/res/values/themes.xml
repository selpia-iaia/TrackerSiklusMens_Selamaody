<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrackerSiklusMenstruasi" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/pink_main</item>
        <item name="colorControlNormal">@color/pink_main</item>
    </style>

    <style name="Theme.TrackerSiklusMenstruasi" parent="Base.Theme.TrackerSiklusMenstruasi">
        <!-- Styling for NumberPicker to match screenshot (Pink selection) -->
        <item name="android:editTextColor">@color/pink_main</item>
        <item name="android:textColorPrimary">@color/pink_main</item>
    </style>

    <style name="KeypadButton" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_columnWeight">1</item>
        <item name="android:backgroundTint">@android:color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">20sp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="cornerRadius">0dp</item>
    </style>
</resources>
